<Page        
    x:Class="SmartHome.Views.LightControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartHome.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="{ThemeResource ButtonPointerOverBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"></RowDefinition>
            <RowDefinition Height="80*"></RowDefinition>
        </Grid.RowDefinitions>

        <Image Source="/LightOn.png"></Image>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock  Text="Intensity" FontSize="30" Margin="25 25 0 0" />
                <Slider Width="300" Maximum="255" Minimum="0" Margin="25 25 0 0" Value="{Binding Intensity,Mode=TwoWay}"/>
                <TextBlock Text="Value" FontSize="30" Margin="25 25 0 0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock  Text="Hue" FontSize="30" Margin="45 25 40 0" />
                <Slider Width="300" Margin="25 25 0 0"/>
                <TextBlock Text="Value" FontSize="30" Margin="25 25 0 0" />
            </StackPanel>
        </StackPanel>

        <Button Command="{Binding BackNavigationCommand}"  Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="25" Height="50" Width="200">Back</Button>

        <ToggleButton IsChecked="{Binding IsOn}" UseLayoutRounding="True" Command="{Binding SwitchLightCommand}" Background="DarkKhaki" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" Height="250" Width="100">ON/OFF</ToggleButton>
    </Grid>



</Page>